# 示例环境变量配置文件
# 复制此文件为 .env 并根据实际情况修改

# ========== 模型配置 ==========
# 模型文件路径（本地路径或 HuggingFace 模型 ID）
MODEL_PATH=./models/deepseek-r1-distill
# 或使用 HuggingFace ID
# MODEL_PATH=deepseek-ai/DeepSeek-R1-Distill-Qwen-7B

# 模型名称（用于 API 调用）
MODEL_NAME=deepseek-audit

# ========== vLLM 服务配置 ==========
# vLLM 服务监听地址
VLLM_HOST=0.0.0.0

# vLLM 服务端口
VLLM_PORT=8000

# GPU 显存利用率（0.0-1.0，推荐 0.9）
GPU_MEMORY_UTILIZATION=0.9

# 最大序列长度（tokens）
MAX_MODEL_LEN=8192

# 数据类型（float16, bfloat16, float32）
DTYPE=bfloat16

# 张量并行大小（多 GPU 时使用）
TENSOR_PARALLEL_SIZE=1

# 量化方法（可选：awq, gptq, squeezellm）
# QUANTIZATION=awq

# ========== Web 界面配置 ==========
# Web 服务监听地址
WEB_HOST=0.0.0.0

# Web 服务端口
WEB_PORT=8501

# Gradio 用户认证（可选）
# GRADIO_USERNAME=admin
# GRADIO_PASSWORD=your_secure_password

# ========== 系统配置 ==========
# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# 系统提示词文件路径
SYSTEM_PROMPT_PATH=./config/system_prompt.txt

# 是否启用 Flash Attention 2（需要硬件支持）
ENABLE_FLASH_ATTN=true

# ========== 高级配置 ==========
# HuggingFace 镜像地址（国内用户）
HF_ENDPOINT=https://hf-mirror.com

# 信任远程代码（某些模型需要）
TRUST_REMOTE_CODE=true

# 最大批处理大小
MAX_BATCH_SIZE=32

# 请求超时时间（秒）
REQUEST_TIMEOUT=300

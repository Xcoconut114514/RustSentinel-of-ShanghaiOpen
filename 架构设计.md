# RustSentinel React 前端 + 数据库架构

## 🏗️ 系统架构

```
┌─────────────────┐
│  React 前端     │
│  (端口: 3000)   │
└────────┬────────┘
         │ HTTP POST
         ↓
┌─────────────────┐
│  后端 API       │
│  (端口: 5000)   │  ← 新增：处理业务逻辑
└────┬───────┬────┘
     │       │
     │       └──────→ 数据库 (SQLite/MySQL)
     │                保存审计历史
     ↓
┌─────────────────┐
│  推理服务       │
│  (端口: 8000)   │  ← 已有：simple_server.py
└─────────────────┘
```

## 📦 技术栈

### 前端
- React + TypeScript
- Axios (HTTP 请求)
- Ant Design / Material-UI (UI 组件)
- Monaco Editor (代码编辑器)

### 后端 API
- Flask / FastAPI
- SQLAlchemy (ORM)
- SQLite / MySQL (数据库)

### 推理服务
- 已有的 simple_server.py

## 🔌 API 设计

### 1. 提交审计请求
```
POST /api/audit
Content-Type: application/json

{
  "code": "rust代码内容",
  "project_name": "项目名称",
  "contract_name": "合约名称"
}

Response:
{
  "audit_id": "uuid",
  "result": "审计报告内容",
  "risk_level": "高危",
  "created_at": "2026-01-11T03:10:00Z"
}
```

### 2. 获取审计历史
```
GET /api/audits?page=1&limit=10

Response:
{
  "total": 100,
  "audits": [
    {
      "id": "uuid",
      "project_name": "项目名称",
      "risk_level": "高危",
      "created_at": "2026-01-11T03:10:00Z"
    }
  ]
}
```

### 3. 获取单个审计详情
```
GET /api/audits/{audit_id}

Response:
{
  "id": "uuid",
  "code": "原始代码",
  "result": "审计报告",
  "risk_level": "高危",
  "created_at": "2026-01-11T03:10:00Z"
}
```

## 💾 数据库设计

### audits 表
```sql
CREATE TABLE audits (
    id VARCHAR(36) PRIMARY KEY,
    project_name VARCHAR(255),
    contract_name VARCHAR(255),
    code TEXT NOT NULL,
    result TEXT NOT NULL,
    risk_level VARCHAR(20),
    duration FLOAT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 📁 项目结构

```
RustSentinel/
├── frontend/              # React 前端
│   ├── src/
│   │   ├── components/
│   │   │   ├── CodeEditor.tsx
│   │   │   ├── AuditResult.tsx
│   │   │   └── HistoryList.tsx
│   │   ├── api/
│   │   │   └── audit.ts
│   │   └── App.tsx
│   └── package.json
│
├── backend/               # Flask 后端 API
│   ├── app.py
│   ├── models.py
│   ├── database.py
│   └── requirements.txt
│
├── simple_server.py       # 推理服务（已有）
└── model/                 # 模型文件（已有）
```
